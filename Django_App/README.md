CRUD web app using django, should have user authentication feature.
Example: create a 'contacts' app, where one can CRUD contact names and number. And user need to authenticate to update/delete contacts.
